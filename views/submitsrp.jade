extends ./layout.bs

block content
  h1 BO-LD Ship Replacement Program
  p Click on the "Submit SRP" button to open the SRP submission form with data filled in.
  p Note that the system fetches new data from zkillboard.com every 10 minutes, so you may not see your most recent loss. In some cases it can take even longer time, but killmails should normally show after a day (if not, they will probably never appear).
  include ./includes/srpPolicy
  .contentsection
    h3 My latest lossmails
    .mylosses(ng-controller="SrpLossListCtrl")
        div(ng-include="'templates/killlist.html'")
        button.more(ng-click="addLosses()") Show more
    